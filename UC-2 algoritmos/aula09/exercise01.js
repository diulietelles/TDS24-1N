// Exercise 1
/* Create a program that has your name and your favorite color
and prints the message: "FULANO's favorite color is COLOR" */

const name = 'Diulie'
const color = 'Blue'

// Perform the exercise twice, using template strings and concatenation
console.log(`The favorite color of ${name} is ${color}`)
console.log("The favorite color of " + name + " is " + color)
